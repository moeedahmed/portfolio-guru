# Pinned to the versions proven on the live Mac Mini venv (2026-07-02).
# The deploy dir auto-installs from this file — floating >= specs let a
# breaking playwright/browser-use release land silently on deploy.
# To upgrade: bump the pin deliberately, run the offline test gate, ship.
fastapi==0.135.1
uvicorn[standard]==0.41.0
browser-use==0.11.13
playwright==1.60.0
pydantic==2.12.5
httpx==0.28.1
# The [job-queue] extra pulls APScheduler, which the bot needs for every
# scheduled task: the liveness heartbeat, retention purge, Stripe
# reconciliation, the weekly digest and the sign-off chase. Without it
# Application.job_queue is None and startup dies on run_daily. The dev
# machine had APScheduler installed by hand, so a plain
# "pip install -r requirements.txt" had been broken here since before
# 2026-08-26 and nobody noticed until a clean environment was built.
python-telegram-bot[job-queue]==22.6
cryptography==46.0.5
sqlmodel==0.0.37
google-generativeai==0.8.6
requests==2.32.5
google-genai==1.67.0
aiosqlite==0.22.1
openai==2.28.0
pytest==9.0.2
pytest-asyncio==1.3.0
pytest-mock==3.15.1
respx==0.22.0
syrupy==5.1.0
telethon==1.42.0
stripe==14.4.1
supabase==2.30.0
portalocker==2.10.1
matplotlib==3.10.9
